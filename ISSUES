No known issues.

Fixed in v1.5.0:
- Device list disappeared from Management > Devices when plugin was installed
- Scan results pagination was stuck on page 1
- Show/hide "No Response" checkbox had no effect
- Large scan result sets could lock up the browser (now server-side paginated)

New in v1.5.0:
- Live scrolling scan feed with real-time IP status display
- Stop scan button to cancel running scans
- Server-side pagination and filtering for scan results
